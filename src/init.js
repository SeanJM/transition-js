var FILTER_DELTA = {
  back : deltaBack,
  bounce : deltaBounce,
  linear : deltaLinear,
  quadratic : deltaQuadratic,
};

var FILTER_EASE = {
  'in' : easeIn,
  'in-out' : easeInOut,
  'out' : easeOut,
};